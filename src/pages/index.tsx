import type { NextPage } from 'next';
import Head from 'next/head';

import { PHONE_NUMBER } from '@constants/compInfo';
import Footer from '@components/Footer';

const Home: NextPage = () => (
  <>
    <h1>test</h1>
    <Footer />
  </>
);

export default Home;
